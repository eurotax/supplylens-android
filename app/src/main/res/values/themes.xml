<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Application Theme (Dark) -->
    <style name="Theme.SupplyLens" parent="android:Theme.Material.NoActionBar">
        <!-- Primary colors -->
        <item name="android:colorPrimary">@color/gold</item>
        <item name="android:colorPrimaryDark">@color/noir_background</item>
        <item name="android:colorAccent">@color/emerald</item>
        
        <!-- Background -->
        <item name="android:colorBackground">@color/background_dark</item>
        
        <!-- Status Bar -->
        <item name="android:statusBarColor">@color/background_dark</item>
        <item name="android:windowLightStatusBar">false</item>
        
        <!-- Navigation Bar -->
        <item name="android:navigationBarColor">@color/background_dark</item>
        <item name="android:windowLightNavigationBar">false</item>
    </style>
    
    <!-- Splash Screen Theme -->
    <style name="Theme.SupplyLens.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/background_dark</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/Theme.SupplyLens</item>
    </style>
</resources>
